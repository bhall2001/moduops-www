{
  "name": "moduops-www",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sst build",
    "clean": "find . -name node_modules -type d -prune -not -path './.git/*' -not -path './.sst/*' -not -path './.worktrees/*' -exec rm -rf {} + && rm -f pnpm-lock.yaml",
    "console": "sst console",
    "deploy": "sst deploy --stage=dev",
    "deploy:prod": "sst deploy --stage=production",
    "dev": "dev",
    "reinstall": "pnpm run clean && pnpm install",
    "remove": "sst remove --stage=dev",
    "remove:prod": "sst remove --stage=production",
    "start:moduops": "pnpm --filter moduops start",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.2",
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "moduops": "workspace:*",
    "sst": "4.17.1"
  }
}
