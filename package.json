{
  "name": "moduops-www",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sst build",
    "console": "sst console",
    "deploy": "sst deploy --stage=dev",
    "deploy:prod": "sst deploy --stage=production",
    "dev": "dev",
    "remove": "sst remove --stage=dev",
    "remove:prod": "sst remove --stage=production",
    "start:moduops": "pnpm --filter moduops start",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.150",
    "@types/node": "^20.19.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "moduops": "workspace:*",
    "sst": "^3.17.6"
  }
}
